1. Merge Sort (Divide & Conquer)

class Solution {
    fun sortArray(nums: IntArray): IntArray {
        ms(nums,0,nums.size - 1)
        return nums
    }

    fun ms(arr:IntArray,left:Int,right:Int){
        if(left >= right) return

        val mid = left + (right - left)/2

        // left sub array
        ms(arr,left,mid)

        // right subarray
        ms(arr,mid+1,right)

        // merge
        merge(arr,left,mid,right)
    }

    fun merge(arr:IntArray,left:Int,mid:Int,right:Int){
        val temp = mutableListOf<Int>()
        var i = left
        var j = mid + 1

        while(i<= mid && j<= right){
            if(arr[i] < arr[j]){
                temp.add(arr[i])
                i++
            }else{
                temp.add(arr[j])
                j++
            }
        }

        // check for i until the end
        while(i <= mid){
             temp.add(arr[i])
                i++
        }

        // check for j until the end
        while(j <= right){
             temp.add(arr[j])
                j++
        }

        // copy the temp array into the 
        for(k in temp.indices){
            arr[left + k] = temp[k]
        }
    }
}


2. Quick sort (Pivot & Partitioning)
/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */
fun main() {
 	val arr = intArrayOf(5,2,6,4,1,3)
    quickSort(arr,0,arr.size - 1)
    print(arr.toList())
}
// step 1
fun quickSort(arr:IntArray,start:Int,end:Int){
    if(start >= end) return
    
    val pivotIndex = partition(arr,start,end)
    
    // left
    quickSort(arr,start,pivotIndex - 1)
    
    // right
    quickSort(arr,pivotIndex + 1, end)
}

// step 2
fun partition(arr:IntArray,start:Int,end:Int):Int{
    var idx = -1
    var pivot = arr[end]
    
    for(j in 0 until end){
        if(arr[j] <= pivot){
            idx++
            swap(arr,idx,j)
        }
    }
    
    idx++
    swap(arr,idx,end)
    
    return idx
}

// step 3
fun swap(arr:IntArray,i:Int,j:Int){
    val tmp = arr[i]
    arr[i] = arr[j]
    arr[j] = tmp
}

