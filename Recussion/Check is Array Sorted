fun main() {
 	val arr = intArrayOf(1,2,8,4,5)
  	println(isSorted(arr,arr.size))
}

fun isSorted(arr:IntArray,n:Int):Boolean{
    if(n == 0 || n == 1) return true
    if(arr[n-2] >= arr[n-1]) return false
    return isSorted(arr,n-1)
}
