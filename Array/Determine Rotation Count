/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */
fun main() {
	val nums = intArrayOf(4,5,6,7,1,2,3)
    val n = nums.size
   
    for(i in nums.indices){
        val j = (i+1) % n
        if(nums[i] > nums[j]){
           return j // output
        }
    }
}
